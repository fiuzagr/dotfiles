{
  "$schema": "https://opencode.ai/config.json",
  "theme": "everforest",
  "lsp": { "clangd": { "disabled": true } },
  "mcp": {
    "MCP_DOCKER": {
      "type": "local",
      "command": ["docker", "mcp", "gateway", "run"],
      "enabled": true
    }
  }
}
